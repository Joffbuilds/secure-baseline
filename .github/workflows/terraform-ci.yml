name: Terraform CI

on:
  push:
    paths:
      - '**.tf'
      - '**.tfvars'
      - '.github/workflows/terraform-ci.yml'
  pull_request:
    paths:
      - '**.tf'
      - '**.tfvars'

jobs:
  terraform:
    name: Terraform Init + Validate + Plan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan

      # - name: Run Sentinel policy checks (optional)
      #   run: |
      #     echo "Sentinel would run here if policies were configured"

      # - name: Run OPA policy tests (optional)
      #   run: |
      #     echo "OPA tests would run here if added"
